{
  "manifest_version": 3,
  "name": "no-consent",
  "version": "0.3.0",
  "description": "Adds a 'Reject all' button — including legitimate interest — to cookie consent banners that don't have one.",
  "host_permissions": ["<all_urls>"],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "run_at": "document_start",
      "all_frames": false,
      "world": "MAIN",
      "js": ["src/rejector.js"]
    }
  ]
}
